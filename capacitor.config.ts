import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Objetivo-Vendas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
